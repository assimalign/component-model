namespace Assimalign.ComponentModel.Cloning;

public enum CloneType
{
    BasicClone = 0, // creates a separate instance of the same 
    DeepClone = 1, // Creates a separate instance of the same type
    FullClone = 2, // 
}
